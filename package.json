{
  "name": "backend",
  "version": "1.0.0",
  "description": "this is for import module",
  "main": "math.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "NityaTripathi",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "generate-random-username": "^2.0.0"
  }
}
